int main()
{   //Fazer um programa para receber valores inteiros X, Y e Z do usuário e determinar se estes valores podem formar os lados de um triângulo. 
    //Em caso afirmativo, informar se o triângulo é equilátero, isósceles ou escaleno.
    int x = 0, y = 0, z = 0; 


    printf("Digite os 3 lados do triangulo: ");
    scanf("%d %d %d", &x, &y, &z);
    
    if (x + y + z == 180) {
        printf("OS VALORES PODEM FORMAR OS LADOS DE UM TRIÂNGULO.\n");
        
        if ((x == y) && (x == z) && (y == z)) {
            printf("\nO TRIÂNGULO É UM EQUILÁTERO.");
            
        }else if (x != y != z) {
            printf("\nO TRIÂNGULO É UM ESCALENO");
            
        }else  {
            printf("\nO TRIANGULO É UM ISÓCELES");
            
        }
     }else {
        printf("\nOS VALORES NÃO FORMAM UM TRIÂNGULO.");
            
    } 
} 
